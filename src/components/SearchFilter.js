import React from 'react';

const SearchFilter = () => (
    <div className="left_container_search_filter">
        <p className="SearchFilter__text">Search filters</p>
        <button className="showButton">Show</button>
    </div>
);

export default SearchFilter;
